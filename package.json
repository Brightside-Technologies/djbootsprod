{
    "name": "djbootsproductions",
    "version": "1.0.0",
    "description": "",
    "scripts": {
        "start": "bundle exec jekyll serve",
        "build": "bundle exec jekyll build",
        "build:dev": "bundle exec jekyll build --config _config.dev.yml"
    }
}